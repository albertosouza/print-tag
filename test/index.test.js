var assert = require('assert');
var sinon = require('sinon');
var PrintTicket = require('../lib');
var printTicket = new Hooks();

describe('hooks-callback', function () {
  it('it should to something!');
});